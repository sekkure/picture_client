import React from 'react'
import WrappedContent from '../../components/WrappedContent'
import styles from './index.module.css'

const MainPage = () => {
  return (
    <WrappedContent className={styles.container}>
      <h1>Main page</h1>
    </WrappedContent>
  )
}

export default MainPage
